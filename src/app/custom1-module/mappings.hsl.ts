// =====================================================================
// Loyola Health Sciences view — feature registrations
// =====================================================================
// `scripts.mjs activate('LUC_HSL')` symlinks this file in as
// `customComponentMappings.ts`. Add feature registrations here as the
// HSL view grows.

export const selectorComponentMap = new Map<string, any>([]);
